package utility

import (
	"example.com/packages/peer"
)

func Print_ledger(peers []*peer.Peer) {

	//p.Ledger.mutex.Lock()
	//l := peers.Le
	//l := ledger.Accounts
	//keys := make([]string, 0, len(l))
	//for k := range l {
	//	keys = append(keys, k)
	//}
	//sort.Strings(keys)
	//println()
	//print("Ledger of: " + p.IpPort + ": ")
	//for _, value := range keys {
	//	print("[" + strconv.Itoa(l[value]) + "]")
	//}
	//println(" with the amount of SignedTransactions: " + strconv.Itoa(p.Ledger.TA) + " and deniedTransactions: " + strconv.Itoa(p.Ledger.Uta))
	////l := p.Ledger.Accounts
	////keys := make([]string, 0, len(l))
	//for k := range l {
	//	keys = append(keys, k)
	//}
	//sort.Strings(keys)
	//
	//println()
	//print("Ledger of: " + p.IpPort + ": ")
	//for _, value := range keys {
	//	print("[" + strconv.Itoa(l[value]) + "]")
	//}
	//println(" with the amount of SignedTransactions: " + strconv.Itoa(p.Ledger.TA) + " and deniedTransactions: " + strconv.Itoa(p.Ledger.Uta))
	////p.Ledger.mutex.Unlock()

}
